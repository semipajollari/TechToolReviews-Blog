# Frontend Environment Variables (prefix with VITE_ for client access)
# Leave VITE_API_URL empty for Vercel deployment (uses same-origin serverless functions)
# Set to external URL only if using separate backend (e.g., Render)
VITE_API_URL=

# Gemini API Key for TechStackAdvisor
VITE_GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Backend Environment Variables (for local development with backend/server.js)
PORT=5000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
NODE_ENV=development
